interface:
  display_name: "Token Firewall Team"
  short_description: "Reuse native Codex agents with Git-backed gates"
  default_prompt: "Use $token-firewall-team to run this task with Codex-native subagents, preserve Git and verification gates, and use an external CLI Adapter only if I explicitly request a third-party platform."
